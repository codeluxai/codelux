{
  "mcpServers": {
    "codelux": {
      "url": "http://localhost:8766/mcp",
      "description": "Codelux local daemon. Install from https://codelux.ai/download and run `codelux start`; the port is the one printed at startup (default 8766)."
    }
  }
}
